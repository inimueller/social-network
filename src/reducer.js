// the reducer has to parameters
// 1. global state
// 2. action

export default function reducer(state = {}, action) {
    // here we will be updating the new state AND THEN returning it

    return state;
}
